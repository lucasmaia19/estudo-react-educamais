import axios from "axios";

export default class CadastroAtividadeService {

    // PostCadastroAtividade(values: FormData) {
        // return axios.post('http://localhost:8080/atividade/upload-com-dados', values)
            // .then(response => {return response})
    // }
};